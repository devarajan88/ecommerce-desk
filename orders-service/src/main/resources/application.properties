spring.application.name=orders-service
# Config Server connection — credentials needed before config can be fetched
spring.config.import=optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
spring.cloud.config.username=configuser
spring.cloud.config.password=${CONFIG_SERVER_PASSWORD:configsecret}

# MongoDB — order history (saga audit trail) stored as append-only documents
# Orders themselves remain in MySQL via JPA (polyglot persistence)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/ordersdb}
spring.data.mongodb.auto-index-creation=true

# GraphQL
spring.graphql.graphiql.enabled=true